// sections
import { ModernNewPasswordView } from '@/sections/auth';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Bolt 360 App: Nova senha',
};

export default function ModernNewPasswordPage() {
  return <ModernNewPasswordView />;
}
