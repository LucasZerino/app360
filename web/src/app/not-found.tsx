// sections
import { NotFoundView } from '@/sections/error';
// layouts

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Bolt 360 App: Página não encontrada',
};

export default function NotFoundPage() {
  return <NotFoundView />;
}
